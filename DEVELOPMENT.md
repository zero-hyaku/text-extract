# 개발 메모

`README.md` 의 기획을 실제로 구현한 코드에 대한 설명입니다.

## 실행

```bash
npm install
npm run dev      # 개발 서버 (http://localhost:5173)
npm run build    # 정적 빌드 → dist/
npm run preview  # 빌드 결과 확인
```

## 기술 선택

| 항목 | 선택 | 이유 |
| --- | --- | --- |
| 프레임워크 | React 18 + TypeScript + Vite | 편집 옵션이 많아 상태가 복잡하고, 미리보기에 실시간 반영이 필요함 |
| 서버 | 없음 (100% 브라우저) | 본문·이미지가 서버로 전송되지 않음. 정적 호스팅만으로 배포 가능 |
| 이미지 저장 | `html-to-image` | 미리보기 DOM 을 그대로 캡처하므로 "보이는 것 = 저장되는 것" |
| PDF 저장 | `jspdf` (동적 import) | PDF 를 실제로 저장할 때만 내려받아 첫 로딩을 가볍게 유지 |
| 저장소 | `localStorage` + JSON 파일 | 자동 저장은 브라우저에, 공유용 서식은 파일로 |

## 구조

```
src/
├── types.ts            편집 옵션 전체의 타입 정의
├── defaults.ts         기본값, 글꼴·비율·색상 프리셋
├── store.tsx           옵션 상태 + localStorage 자동 저장
├── styles.css          앱 UI 와 미리보기 스타일
├── lib/
│   ├── parse.ts        대사/서술 인식, 캐릭터 이름 찾기
│   ├── caret.ts        본문을 다시 그릴 때 커서 위치 보존
│   ├── format.ts       선택 영역 서식 (볼드·색·하이라이트 등)
│   ├── exporters.ts    PNG / JPG / PDF 저장, 클립보드 복사
│   └── presets.ts      서식 슬롯 저장, 파일 내보내기·불러오기
└── components/
    ├── App.tsx         전체 레이아웃
    ├── Workspace.tsx   아이콘 레일 + 꺼내 쓰는 패널
    ├── Preview.tsx     내보내기 대상이 되는 이미지 영역
    ├── Editor.tsx      본문 입력 (contenteditable)
    ├── Stickers.tsx    본문 위에 얹는 이미지
    ├── SelectionPopup.tsx 드래그 선택 시 뜨는 편집 팝업
    └── panels/         옵션 패널 7종
```

## 구현 시 결정한 것들

### 색이 일부만 바뀌던 문제

`--te-ink` 동기화에서 역할 span 을 건너뛰면 안 됩니다. 브라우저는 선택 영역이 역할
span 과 정확히 겹칠 때 그 span 에 바로 색을 얹는데, 변수를 같이 심어 두지 않으면
재파싱에서 새로 만들어진 안쪽 역할 span 이 자기 기본색으로 되돌아갑니다.
그래서 여러 문단을 한 번에 칠했을 때 "일부만" 바뀐 것처럼 보였습니다.
재파싱 직후에도 `syncInlineVars` 를 한 번 더 돌립니다.

### 내보내기가 되다 말다 하던 문제

`html-to-image` 는 첫 호출에서 글꼴·이미지를 인라인하는 캐시를 채웁니다. 그래서 첫
결과가 비거나 일부가 빠지는 일이 잦습니다. 작은 배율로 한 번 버리고 다시 찍습니다.
이미지도 `decode()` 까지 기다립니다.

외부 주소 이미지는 보안 정책 때문에 캡처에 담기지 않습니다. 배경 패널의
`저장에 포함되도록 받아 오기` 로 미리 data: URL 로 바꿔 둡니다.

### 초기화가 되돌리는 범위

'모든 서식 초기화' 는 **서식만** 되돌립니다. 다크/라이트 모드, 도구 위치·너비,
열려 있던 패널, 확대 배율은 서식이 아니라 작업 환경이라 그대로 둡니다.
초기화했다고 화면 모드가 바뀌면 당황스럽기 때문입니다.

### 서식 초기화가 새로고침하면 되살아나던 문제

`stripAllFormatting` 은 DOM 을 직접 바꾸므로 React 가 모릅니다. 끝나고
`input` 이벤트를 쏘아 저장된 본문까지 갱신해야 합니다.

### 드래그 서식이 사이드바 서식을 이기는 방법

사이드바의 역할 색·크기는 `color: var(--te-ink, var(--te-dialogue))` 처럼 읽습니다.
드래그로 준 서식은 `--te-ink` / `--te-size` 를 함께 심어두므로, 상속을 타고 내려가
안쪽 역할 span 까지 덮어씁니다. 바깥/안쪽 중첩 순서와 무관하게 드래그 서식이 항상 이깁니다.

역할 span 을 다시 씌울 때는 `style` 이 남아 있는 span 을 평범한 span 으로 옮겨 보존합니다.
브라우저가 선택 영역과 역할 span 이 정확히 겹칠 때 새 요소를 만들지 않고
그 span 에 바로 style 을 얹기 때문에, 그냥 벗겨내면 방금 준 서식이 함께 사라집니다.

### 글자 크기

`execCommand('fontSize')` 는 styleWithCSS 가 켜져 있으면 `xxx-large` 같은 키워드를 넣어
이후 크기 조절이 먹지 않습니다. 반드시 styleWithCSS 를 끄고 `<font size="7">` 를 만든 뒤
px span 으로 바꿔칩니다.

크기 조절 대상은 선택 여부로 갈립니다: 드래그한 영역이 있으면 그 영역만, 없으면 본문 전체
(서술/대사 기준값). 사이드바에는 서술 크기와 대사 크기가 따로 있습니다.

### 대사 / 서술 인식

`lib/parse.ts` 가 본문 텍스트를 훑으면서 역할별로 `<span>` 을 씌웁니다.

- 따옴표(`" "`, `“ ”`, `「 」`, `『 』`) 안은 **대사**. 따옴표가 닫힐 때까지 이어지므로
  **줄바꿈을 넘어가는 대사도 하나로 인식**합니다.
- `*…*` / `**…**` 안은 **강조 서술**.
- 줄 머리의 `이름:` 은 **캐릭터 이름**으로 보고, 이후 대사에 화자를 표시합니다.
- 나머지는 **서술**.

색상은 span 에 직접 칠하지 않고 CSS 변수(`--te-dialogue` 등)로 넘깁니다.
그래서 색을 바꿔도 본문을 다시 파싱하지 않고 즉시 반영되며,
사용자가 드래그해서 직접 준 색이 지워지지 않습니다.

한글 입력(IME) 조합 중에는 다시 그리지 않고, 결과가 달라질 때만 DOM 을 교체한 뒤
`lib/caret.ts` 로 커서를 원래 자리에 돌려놓습니다.

### 장평

CSS `font-stretch` 는 가변 글꼴이 아니면 동작하지 않아, 본문 영역을 `1/배율` 폭으로
넓혀 줄바꿈을 계산한 뒤 `scaleX()` 로 눌러 실제 폭에 맞춥니다.
그래서 장평을 바꾸면 줄바꿈 위치도 함께 바뀝니다.

### 이미지 영역 비율

지정한 비율은 **최소 크기**로 적용됩니다(`min-height`).
본문이 비율보다 길면 세로로 늘어나므로 글이 잘리지 않습니다.
(README 의 "너비를 지정하지 않아도 본문에 따라 유동적으로 늘어난다" 요구사항)

### 강조 기호 숨기기

`*…*` 의 별표는 따로 떼어 `data-te-role="mark"` span 으로 감싸고, 결과물에서는
`display: none` 으로 자리까지 없앱니다 (사이드바에서 끌 수 있음).

여는 기호를 막 입력했을 때 글자가 사라지면 곤란하므로, **닫는 짝이 같은 줄에
있을 때만** 강조로 인식합니다. `3 * 4` 처럼 홀로 쓴 별표는 그냥 글자로 남습니다.

### 화면 구조

포토샵처럼 **왼쪽 아이콘 레일 + 꺼내 쓰는 패널 하나** 구조입니다.

- 레일 차례는 만드는 순서를 따릅니다: 제목·제작자 → 본문 → 색상 → 말풍선·캐릭터
  → 배경 → 이미지 영역 → 저장·설정.
- 캐릭터는 말풍선 패널에서 다룹니다. 이름·대사 색뿐 아니라 말풍선 색까지 함께
  정하는 자리라 한곳에 모아 두는 편이 헷갈리지 않습니다.
- 레일 아이콘을 누르면 그 패널만 열리고, 같은 아이콘을 다시 누르면 닫힙니다.
  아이콘만으로는 뭔지 모르므로 hover 하면 이름표가 뜹니다.
- 열려 있는 패널은 `settings.activePanel` 에 남아 다음에 열 때도 그대로입니다.
- 패널 오른쪽 가장자리를 드래그해 폭을 조절합니다(280–640px, 더블클릭하면 기본값).
  폭이 470px 을 넘으면 값 조절·색상 항목이 2열로 늘어섭니다(컨테이너 쿼리).
- 좁은 화면(900px 이하)에서는 패널이 작업 영역을 덮도록 띄웁니다.

상단 막대 오른쪽에 **되돌리기·다시 실행·다크/라이트** 를 둡니다.
스크롤과 무관하게 늘 같은 자리에 있습니다. 되돌리기는 Ctrl+Z / Ctrl+Shift+Z 로도 됩니다.

앱 화면 색은 `:root[data-app-theme='light']` 에서 토큰만 다시 정의하며,
결과 이미지 색과는 무관합니다.

### 화면의 결

**작업대처럼 보이게** 짭니다. 카드와 그림자로 요소를 띄우는 대신

- 1px 실선으로 칸을 나누고 (상단 막대·레일·패널이 같은 선으로 이어집니다),
- 모서리는 거의 각지게 (`--r-sm: 2px`),
- 강조는 파랑 한 가지로만 줍니다.

편집기라 하루 종일 들여다보는 화면이므로, 색을 아끼고 구조를 또렷하게 하는 편이
눈이 덜 피곤합니다. 그렇다고 모든 요소에 테두리를 두르지는 않습니다 —
안내 문구는 왼쪽에 선 하나만 세우고, 묶음의 머리에는 작은 파란 눈금만 붙입니다.

작업 영역은 모눈 바탕에 네 귀퉁이 맞춤 표시를 두어 '판' 이라는 것을 알려 줍니다.
패널 머리의 `01`–`07` 은 레일의 몇 번째 칸인지를 가리킵니다.

글꼴은 화면·결과물 모두 **프리텐다드** 입니다.

### 아이콘

직접 그리지 않고 **Font Awesome** 의 모양을 가져다 씁니다. 손으로 그린 선 아이콘은
굵기·여백이 제각각이라 나란히 두면 눈에 거슬립니다.

다만 아이콘 글꼴도, 공식 렌더러(`@fortawesome/react-fontawesome`)도 쓰지 않습니다.
렌더러는 온갖 기능이 딸려 와 gzip 28KB 를 더하는데 우리가 쓰는 건 열두 모양뿐입니다.
모양 자료(`free-solid-svg-icons`)만 가져와 `src/components/icons.tsx` 에서 직접
그립니다 — 번들에는 **쓰는 모양만** 남아, 열두 개를 더해도 gzip 2.6KB 입니다.

### 제목과 제작자의 위치

둘은 각자 **본문 위 / 본문 아래** 를 따로 고릅니다(`meta.position`, `meta.authorPosition`).
같은 쪽을 고르면 제작자가 바깥쪽에 놓입니다. 제목은 본문을 소개하는 말이고
제작자는 작업 전체에 붙는 서명이라, 제작자가 더 바깥에 오는 쪽이 자연스럽습니다.

간격(`authorGap`)은 본문을 바라보는 쪽에만 줍니다. 위에 있으면 `margin-bottom`,
아래에 있으면 `margin-top` 이라 위치를 바꿔도 사이가 벌어진 느낌이 같습니다.

### 확대 / 축소

`transform: scale` 은 레이아웃 크기를 바꾸지 않습니다. 확대한 만큼 바깥 틀의 높이를
따로 잡아 주지 않으면 미리보기가 아래 도구 막대를 덮어버립니다.
`ResizeObserver` 로 실제 높이를 재서 `높이 × 배율` 로 자리를 만듭니다.
결과물 크기와는 무관하며, 저장에는 영향을 주지 않습니다.

### 스티커

본문 위에 얹는 이미지입니다. 미리보기에서 바로 끌어 옮기고, 오른쪽 아래 손잡이로
크기를, 위쪽 손잡이로 각도를 바꿉니다(Shift 를 누르면 15도 단위, 더블클릭하면 0도).
각도는 손잡이가 아니라 **이미지 중심**을 축으로 잽니다 — 손잡이를 축으로 삼으면
손잡이를 잡을 때마다 기준이 달라져 회전이 튑니다.

위치·크기는 결과물 너비 대비 % 로 들고 있어 화면을 확대·축소해도 흔들리지 않습니다.
손잡이와 테두리에는 `data-export-ignore` 를 달아 저장한 그림에는 나오지 않습니다.

### 팝업 안의 입력칸

팝업의 숫자 입력이나 드롭다운에 포커스가 가면 본문 선택이 풀려 팝업이 닫힙니다.
마지막 선택 Range 를 들고 있다가 명령 직전에 되돌려 놓고, 포커스가 팝업 안에 있는 동안은
닫지 않습니다.

### 본문을 평문으로 읽기

`innerText` 를 쓰면 화면에서 감춘 강조 기호(*)까지 빠져, 어디가 강조인지
알 수 없게 됩니다. 그래서 블록을 직접 훑어 줄 단위로 읽습니다(`readPlainText`).

### 되돌리기

브라우저 기본 실행취소는 본문 글자만 되돌리고 사이드바 서식은 그대로 둡니다.
본문 HTML 과 설정을 한 덩어리(Snapshot)로 묶어 함께 되돌립니다. 복원하는 동안에는
`restoring` 을 세워 기록이 다시 쌓이지 않게 합니다.

### 페이지 나눔

본문에 `data-te-page-break` 블록을 넣으면, 저장할 때 그 선을 기준으로 여러 장으로 쪼갭니다.
줄마다 `display` 를 껐다 켜며 한 장씩 캡처하고, 끝나면 반드시 되돌립니다.
contenteditable 이 첫 줄을 `<div>` 로 감싸지 않는 경우가 있어, 맨 위 텍스트 노드는
임시 span 으로 싸 두었다가 캡처 후 풀어 줍니다. PDF 는 한 파일에 여러 쪽으로 들어갑니다.

### 작업 내용 보관

localStorage 는 한 사이트당 5MB 안팎입니다. 배경 사진 한 장이면 한도를 넘고, 넘는
순간 **아무 소리 없이** 저장이 멈춰 새로고침할 때 작업이 통째로 사라집니다.
그래서 설정과 본문을 IndexedDB 에 담습니다. 예전 localStorage 기록은 첫 실행 때
한 번만 옮겨 옵니다. 그래도 저장이 막히면 화면 위에 경고를 띄웁니다.

읽기가 비동기라, **다 읽기 전에는 화면을 만들지 않습니다**(`StoreProvider` 가 막습니다).
에디터는 마운트할 때 내용을 한 번만 받으므로, 먼저 그려 버리면 빈 본문을 붙잡은 채
시작해 저장된 글을 덮어씁니다.

### 사용자 글꼴

한글 글꼴 파일은 보통 수 MB 라 localStorage 한도에 바로 걸립니다. 파일은 IndexedDB 에 두고
설정에는 목록만 남긴 뒤, 불러올 때 data: URL 로 `@font-face` 를 심습니다.
data: URL 이어야 내보내기(html-to-image)에서도 글꼴이 그대로 살아납니다.

### 사이드바 2열 배치

`.sidebar-scroll` 에 `container-type: inline-size` 를 걸고 컨테이너 쿼리로 전환합니다.
폭이 470px 을 넘으면 값 조절·색상 항목만 2열로 늘어서고, 나머지는 한 줄을 다 씁니다.

### 앱 색상 토큰

앱 화면 색은 전부 `--ui-*` 토큰을 거칩니다. 라이트 모드는 `:root[data-app-theme='light']`
에서 토큰만 다시 정의합니다. **최상위 요소**에 걸어야 `body` 가 물려주는 색까지 함께
바뀝니다 — `.app` 에만 걸면 body 를 그냥 상속받는 글자가 다크 모드 색으로 남습니다. 팝업·hover·토글처럼 눈에 잘 안 띄는 곳까지 토큰을 쓰지 않으면
한쪽 모드에서만 색이 어긋나므로, 새 UI 를 만들 때 색을 직접 적지 않습니다.

### 빈 줄 처리

문단 사이 간격은 `문단 간격` 옵션이 담당합니다. 붙여넣은 글의 빈 줄까지 그대로 두면
간격이 두 배로 벌어지므로, 기본적으로 붙여넣을 때 빈 줄을 정리합니다.
사이드바 위쪽의 `붙여넣을 때 빈 줄 정리` 로 끌 수 있습니다.

### 배경 영상 / GIF

- **위치**: `드래그로 위치 조절` 을 켜면 미리보기 위에 덮개가 생기고, 끌어서 이미지 위치를
  옮깁니다. 켜져 있는 동안에는 본문 편집이 막힙니다. `꽉 채우기` 일 때 가장 잘 보입니다.
- **영상**: 저장 시점의 재생 프레임을 캔버스로 떠서 캡처합니다. 원하는 장면에서 저장하세요.
- **GIF**: 미리보기에서는 움직이지만 이미지·PDF 로 저장하면 한 장면으로 고정됩니다.
- 외부 주소의 영상·이미지는 CORS 설정에 따라 캡처가 막힐 수 있습니다. 업로드 방식이 안전합니다.

### 말풍선과 대본

본문에서 글을 드래그하고 팝업의 **말풍선** 또는 **대본** 버튼을 누르면 그 모양이 됩니다.
인물을 고르면 그 캐릭터의 색이 함께 붙고, 같은 자리에서 인물을 바꾸거나 풀 수 있습니다.

둘은 겉모양만 다르고 얼개가 같습니다 — 겉을 감싸는 상자 + 이름표 + 본문
(`wrapSelection` 하나로 만듭니다).

- **말풍선**: 이름·프로필·말풍선 색·말풍선 글자색까지 씁니다.
- **대본**: 이름 칸과 대사 칸을 나란히 둡니다. 특수문자로 띄우지 않고 칸으로 나누므로,
  대사가 길어 줄이 넘어가도 둘째 줄이 이름 아래로 파고들지 않고 대사 칸에 붙습니다.
  말풍선 색은 쓰지 않고 **이름·대사 색만** 씁니다.

겉모양은 `applyBubble`/`applyScript` 가 정하지 않습니다 — `data-te-speaker` 만 달고,
색은 CSS 가 이름을 보고 붙입니다. 색을 요소에 박아 두면 나중에 캐릭터 색을 바꿔도
이미 만들어 둔 것이 따라오지 않습니다.

이름줄(말풍선)은 `[프로필] [이름]` 두 칸짜리 flex 입니다. '내 쪽' 은 `row-reverse`
한 줄로 `[이름] [프로필]` 이 되고(오른쪽에 붙는 말풍선이라 이 차례라야 오른쪽 끝이
가지런합니다), 프로필을 끄면 `display:none` 으로 **자리까지** 함께 사라집니다.
꼬리 쪽 모서리(`--bub-tail`)는 2px — 각지면 꼬리로 보이고 둥글면 그냥 둥근 상자가
되는데, 2px 는 '깎다 만' 정도라 꼬리는 그대로 보이면서 날카롭지 않습니다.

### 이름표는 글이 아니다

말풍선·대본이 달고 있는 이름표에는 `data-te-label` 이 붙습니다.
우리가 붙인 표지지 사용자가 쓴 글이 아니므로, 본문을 평문으로 읽을 때
(`readPlainText`)와 역할을 다시 인식할 때(`markupRoles`) 건너뜁니다.

건너뛰지 않으면 `세인: "대사"` 를 말풍선으로 만든 줄이 `세인: 세인"대사"` 로 읽혀
이름·대사 인식이 어긋납니다. 말풍선 서식이 '먹다 말다' 하던 원인 중 하나였습니다.

`이름: "대사"` 를 통째로 드래그하면 이름이 두 번 보입니다(이름표 + 본문 속 `이름:`).
같은 인물일 때만 본문 쪽을 감춥니다.

### 글자색이 정해지는 차례

색이 정해지는 자리가 여럿이라(공용 역할색, 캐릭터별 색, 말풍선·대본 상자, 드래그로
준 색) **차례를 한 줄에 모아 두었습니다.** `styles.css` 의 역할 span 규칙이 그것입니다.

    --te-box-ink   상자(말풍선·대본) 안이면 그 상자의 글자색
    --te-name-ink  등록한 캐릭터의 이름 색
    --te-say-ink   등록한 캐릭터의 대사 색
    --te-ink       드래그해서 준 색
    그 외          색상 패널의 역할별 공통색

색을 요소에 직접 넣지 않고 **변수로 넘기는 것**이 핵심입니다.

예전에는 상자가 `color` 를 갖고 그 안의 역할 span 이 또 제 `color` 를 가졌습니다.
CSS 는 상속보다 제 선언을 먼저 보므로 **언제나 가장 안쪽이 이겼고**, 말풍선을
만들어도 안쪽 대사 span 이 공용 대사색을 그대로 써 버렸습니다. 캐릭터 색이 먹다 말다
하던 일이 되풀이된 뿌리가 이것입니다. 겉 상자만 재 보면 맞게 나오니 눈에도,
시험에도 잘 드러나지 않았습니다 — **색을 확인할 때는 글자를 담은 가장 안쪽 요소를
재야 합니다.**

캐릭터·상자가 드래그 색보다 앞섭니다. 캐릭터를 따로 두는 까닭이 색을 분리하는
것인데, 본문을 통째로 드래그하면 그게 전부 묻혔기 때문입니다. 그 대신 **상자 안이나
등록한 캐릭터의 이름·대사는 드래그로 색을 바꿀 수 없습니다.**

### 색을 정하는 자리는 적을수록 낫다

한 캐릭터가 갖는 색은 **이름 · 대사 · 말풍선** 셋뿐입니다.

- **말풍선 안 글자색은 따로 없습니다.** '대사' 색을 그대로 씁니다.
  예전에는 '말풍선 글자' 를 따로 두었는데, 대사 색과 뜻이 겹쳐 어느 쪽이 먹는지
  헷갈렸습니다. 대본 안 글자도 마찬가지로 대사 색입니다.
- **'내 말풍선 색' 도 없앴습니다.** 캐릭터가 저마다 말풍선 색을 가지므로 한 번도
  쓰이지 않는 설정이었습니다. '내 쪽' 은 이제 좌우와 꼬리 방향만 바꿉니다.
- 말풍선 패널의 '말풍선 색' 은 **인물을 고르지 않은** 말풍선에만 쓰입니다.

캐릭터의 색은 만들 때의 공통값을 **그대로 떠서** 제 값으로 갖습니다(`newCharacter`).
비워 두고 물려받게 하면 나중에 공통값을 바꿀 때 캐릭터까지 끌려갑니다.
예전에 비워 둔 채 저장된 캐릭터는 불러올 때 굳혀 줍니다.

### 가볍게 만들기

두 군데가 무거웠습니다.

**글꼴** — 한글 글꼴 여덟 벌을 첫 화면에서 미리 받고 있었습니다. 한 벌이 수 MB 라
첫 화면이 그만큼 늦고, 이미지로 저장할 때 html-to-image 가 **쓰지도 않는 글꼴까지**
전부 base64 로 그림 안에 끼워 넣었습니다. 고른 글꼴만 그때 받아 옵니다
(`src/lib/webfonts.ts`).

**저장** — 두 가지를 고쳤습니다.

- 캡처 전에 작게 한 번 찍어 버리는 '예열' 이 있었습니다(첫 결과가 비는 일이 잦아서).
  원인은 글꼴 끼워 넣기 준비가 첫 호출에서야 이뤄지는 것이었으므로,
  `getFontEmbedCSS` 로 한 번 만들어 두고 넘깁니다. 버리는 촬영이 사라졌습니다.
- 그 CSS 를 세션 내내 돌려 씁니다. PDF 는 쪽마다 글꼴을 다시 끼워 넣고 있었습니다.

400줄 글 기준 17초 → 8.7초, 같은 글을 이어서 저장하면 0.15초입니다.
연달아 다섯 번 저장해 바이트까지 같은 그림이 나오는 것을 확인했습니다.

### 서술 색은 본문 바탕색이기도 하다

'대사 / 서술 자동 구분' 을 끄면 역할 span 이 없어 본문 바탕색만 남습니다.
그 바탕색이 고정값이어서, 구분을 끈 사람에게는 서술 색을 아무리 바꿔도
화면이 그대로였습니다. 언제나 `roles.narration` 을 씁니다.

### 드래그 팝업의 자리

화면(뷰포트) 좌표를 그대로 쓰고 `position: fixed` 입니다. 예전에는 작업 영역 기준으로
계산했는데, 팝업이 실제로 매달린 상자는 **스크롤되는 쪽**이라 스크롤한 만큼 어긋나
한참 아래(가운데쯤)에 떨어져 있었습니다. 화면 좌표면 매달린 상자가 무엇이든
상관없습니다 — 스크롤·확대·창 크기가 바뀔 때마다 다시 잡습니다.

자리는 언제나 드래그한 글 **바로 위**입니다. 위로 올렸을 때 작업 영역 윗변에 걸려
잘릴 때에만 아래로 내립니다. 팝업 높이는 열린 하위 패널에 따라 달라지므로 어림수가
아니라 실제 높이를 재고, 패널을 여닫으면 그리자마자 자리를 다시 잡습니다.
드래그한 글이 화면 밖으로 스크롤돼 나가면 팝업도 함께 감춥니다.

### 고른 이미지의 파란 테두리

'지금 골라 둔 것' 을 보여 주는 화면 안내지 결과물이 아닙니다. 그런데 테두리를
이미지 자신에게 `outline` 으로 걸어 두어, `data-export-ignore` 로는 걸러지지
않았습니다(그 속성은 요소를 통째로 빼는 장치입니다). 저장 직전에 표시를
잠깐 떼었다가 되돌립니다.

표시를 클릭할 때만 지우면 다른 곳을 드래그해도 테두리가 남습니다.
`selectionchange` 를 듣고 선택이 바뀔 때마다 맞춥니다.

### 커서 위치를 재는 법

글자 수 하나로만 재면 **빈 줄을 가리킬 수 없습니다**. 빈 줄은 글자가 0개라 바로 앞
줄 끝과 위치가 똑같아지고, 다시 그릴 때 커서가 앞 줄로 빨려 들어갑니다.
(새 줄을 만들고 타이핑하면 글이 앞 줄로 들어가던 문제가 이것이었습니다.)
그래서 `{ line, offset }` — 몇 번째 줄인지와 그 줄 안에서 몇 글자째인지 — 를 함께 듭니다.

### 줄을 우리가 직접 나누는 이유

말풍선은 `div` 라, 줄 안에 블록이 들어앉습니다. 그 상태에서 브라우저는 문단을
나누지 못하고 **그냥 무시합니다** — 여러 줄을 붙여넣으면 한 줄에 다 붙었습니다.
그래서 `splitLineAtCaret` 으로 줄을 직접 나눕니다.

맨 위에 덩그러니 놓인 `<br>` (전체 지우기 뒤 브라우저가 남기는 것)은 줄이 아니라
빈 줄 표시라, 줄로 착각하고 글을 넣으면 `<br>` 안으로 들어가 화면에서 사라집니다 —
줄로 감싸고 나서 다룹니다.

말풍선 **안에서** Enter 를 누르면 말풍선 밖 새 줄로 빠져나옵니다. 그냥 두면
브라우저가 말풍선을 통째로 복제해 아래 줄까지 말풍선이 됩니다.
말풍선 안에서 줄을 바꿀 때는 Shift+Enter 입니다.

### 괄호 안은 곁말

`(…)` 안의 글은 `te-paren` 조각으로 따로 떼어, **대사 크기보다 2px 작은 회색**으로
두고 괄호 기호는 감춥니다 (저장 · 설정 > 편집 설정에서 끌 수 있습니다).
크기는 `--te-paren-size` 로 넘기므로 대사 크기를 바꾸면 곁말도 같이 움직입니다.

`*…*` 과 같은 규칙으로 **닫는 괄호가 같은 줄에 있을 때만** 곁말로 봅니다.
그래야 여는 괄호를 막 입력한 순간 뒷글자가 통째로 작아지지 않습니다.

대사 구분을 끄면(`roles-off`) 이 서식도 함께 물러납니다. 색만 풀고 괄호는
계속 감추면, 글자가 소리 없이 사라진 것처럼 보이기 때문입니다.

### 감춘 기호는 지워지지 않는다

`*` 과 `(` `)` 를 `display:none` 으로 감췄더니, 본문을 전체 선택해서 지워도
줄 끝의 기호 한 글자가 살아남았습니다. 브라우저의 전체 선택이 보이지 않는
요소를 건너뛰기 때문입니다.

한때 `font-size: 0` 으로 눌러 두는 방법을 썼는데, **그 편이 훨씬 나빴습니다.**
지운 자리의 '이어서 칠 서식' 이 크기 0 으로 굳어, 그 뒤에 치는 글이 전부
보이지 않게 됐습니다 (`<span style="font-size: 0px">새로 쓴 글</span>`).
게다가 html-to-image 는 글자 크기를 옮길 때 `floor(값) - 0.1` 로 줄여 적는데
(글이 접히는 것을 막으려는 장치입니다) `0px` 은 `-0.1px` 이라는 잘못된 값이
되어 버려지고, **저장한 그림에서만 괄호가 되살아났습니다.**

그래서 감추는 일은 `display:none` 에 맡기고, 남는 기호는 세 군데서 걷습니다.

1. Ctrl/Cmd+A 를 우리가 받아 본문 전체를 범위로 잡습니다. 감춘 것까지 들어오므로
   이어지는 붙여넣기·지우기가 깨끗합니다.
2. 짝(여는·닫는)이 둘이 아닌 괄호 묶음은 지우다 만 자리입니다 — 글은 두고
   기호만 떨굽니다.
3. 감춘 기호만 남았으면 글이 하나도 없는 것이니 빈 본문으로 봅니다.

### 드래그 팝업의 폭은 못 박는다

팝업은 `position: fixed` 에 `left` 만 주므로, 아무 폭도 정하지 않으면 '남은
자리에 맞춰 줄어드는' 상자가 됩니다. 화면 오른쪽을 드래그할수록 좁아지면서
버튼이 여러 줄로 접혔습니다. 같은 도구가 드래그한 곳에 따라 다른 모양으로
나오면 손이 자리를 기억하지 못합니다. `width: min(568px, calc(100vw - 20px))`
로 못 박아 두고, 화면 밖으로 나가는 것은 원래 하던 대로 자리 계산에서 막습니다.

## 배포

`main` 브랜치에 푸시하면 `.github/workflows/deploy.yml` 이 GitHub Pages 로 배포합니다.
저장소 **Settings → Pages → Source** 를 **GitHub Actions** 로 한 번 설정해야 동작합니다.

프로젝트 페이지(`/text-extract/`) 기준으로 경로가 잡혀 있습니다.
다른 경로에 올리려면 `VITE_BASE` 환경변수로 덮어쓰세요.

## 아직 안 된 것

- 이미지 영역(결과물) 크기를 **드래그 핸들**로 조절하는 기능 (지금은 숫자·슬라이더만)
- 본문 편집 내역 되돌리기는 브라우저 기본 실행취소(Ctrl+Z)에 의존
