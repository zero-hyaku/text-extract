# 개발 메모

`README.md` 의 기획을 실제로 구현한 코드에 대한 설명입니다.

## 실행

```bash
npm install
npm run dev      # 개발 서버 (http://localhost:5173)
npm run build    # 정적 빌드 → dist/
npm run preview  # 빌드 결과 확인
```

## 기술 선택

| 항목 | 선택 | 이유 |
| --- | --- | --- |
| 프레임워크 | React 18 + TypeScript + Vite | 편집 옵션이 많아 상태가 복잡하고, 미리보기에 실시간 반영이 필요함 |
| 서버 | 없음 (100% 브라우저) | 본문·이미지가 서버로 전송되지 않음. 정적 호스팅만으로 배포 가능 |
| 이미지 저장 | `html-to-image` | 미리보기 DOM 을 그대로 캡처하므로 "보이는 것 = 저장되는 것" |
| PDF 저장 | `jspdf` (동적 import) | PDF 를 실제로 저장할 때만 내려받아 첫 로딩을 가볍게 유지 |
| 저장소 | `localStorage` + JSON 파일 | 자동 저장은 브라우저에, 공유용 서식은 파일로 |

## 구조

```
src/
├── types.ts            편집 옵션 전체의 타입 정의
├── defaults.ts         기본값, 글꼴·비율·색상 프리셋
├── store.tsx           옵션 상태 + localStorage 자동 저장
├── styles.css          앱 UI 와 미리보기 스타일
├── lib/
│   ├── parse.ts        대사/서술 인식, 메신저용 구조 파싱
│   ├── caret.ts        본문을 다시 그릴 때 커서 위치 보존
│   ├── format.ts       선택 영역 서식 (볼드·색·하이라이트 등)
│   ├── exporters.ts    PNG / JPG / PDF 저장, 클립보드 복사
│   └── presets.ts      서식 슬롯 저장, 파일 내보내기·불러오기
└── components/
    ├── App.tsx         전체 레이아웃
    ├── Sidebar.tsx     편집 옵션 패널 (좌/우 위치 전환)
    ├── Preview.tsx     내보내기 대상이 되는 이미지 영역
    ├── Editor.tsx      본문 입력 (contenteditable)
    ├── MessengerView.tsx  메신저 테마 렌더링
    ├── SelectionPopup.tsx 드래그 선택 시 뜨는 편집 팝업
    └── panels/         옵션 패널 7종
```

## 구현 시 결정한 것들

### 색이 일부만 바뀌던 문제

`--te-ink` 동기화에서 역할 span 을 건너뛰면 안 됩니다. 브라우저는 선택 영역이 역할
span 과 정확히 겹칠 때 그 span 에 바로 색을 얹는데, 변수를 같이 심어 두지 않으면
재파싱에서 새로 만들어진 안쪽 역할 span 이 자기 기본색으로 되돌아갑니다.
그래서 여러 문단을 한 번에 칠했을 때 "일부만" 바뀐 것처럼 보였습니다.
재파싱 직후에도 `syncInlineVars` 를 한 번 더 돌립니다.

### 내보내기가 되다 말다 하던 문제

`html-to-image` 는 첫 호출에서 글꼴·이미지를 인라인하는 캐시를 채웁니다. 그래서 첫
결과가 비거나 일부가 빠지는 일이 잦습니다. 작은 배율로 한 번 버리고 다시 찍습니다.
이미지도 `decode()` 까지 기다립니다.

외부 주소 이미지는 보안 정책 때문에 캡처에 담기지 않습니다. 배경 패널의
`저장에 포함되도록 받아 오기` 로 미리 data: URL 로 바꿔 둡니다.

### 서식 초기화가 새로고침하면 되살아나던 문제

`stripAllFormatting` 은 DOM 을 직접 바꾸므로 React 가 모릅니다. 끝나고
`input` 이벤트를 쏘아 저장된 본문까지 갱신해야 합니다.

### 드래그 서식이 사이드바 서식을 이기는 방법

사이드바의 역할 색·크기는 `color: var(--te-ink, var(--te-dialogue))` 처럼 읽습니다.
드래그로 준 서식은 `--te-ink` / `--te-size` 를 함께 심어두므로, 상속을 타고 내려가
안쪽 역할 span 까지 덮어씁니다. 바깥/안쪽 중첩 순서와 무관하게 드래그 서식이 항상 이깁니다.

역할 span 을 다시 씌울 때는 `style` 이 남아 있는 span 을 평범한 span 으로 옮겨 보존합니다.
브라우저가 선택 영역과 역할 span 이 정확히 겹칠 때 새 요소를 만들지 않고
그 span 에 바로 style 을 얹기 때문에, 그냥 벗겨내면 방금 준 서식이 함께 사라집니다.

### 글자 크기

`execCommand('fontSize')` 는 styleWithCSS 가 켜져 있으면 `xxx-large` 같은 키워드를 넣어
이후 크기 조절이 먹지 않습니다. 반드시 styleWithCSS 를 끄고 `<font size="7">` 를 만든 뒤
px span 으로 바꿔칩니다.

크기 조절 대상은 선택 여부로 갈립니다: 드래그한 영역이 있으면 그 영역만, 없으면 본문 전체
(서술/대사 기준값). 사이드바에는 서술 크기와 대사 크기가 따로 있습니다.

### 대사 / 서술 인식

`lib/parse.ts` 가 본문 텍스트를 훑으면서 역할별로 `<span>` 을 씌웁니다.

- 따옴표(`" "`, `“ ”`, `「 」`, `『 』`) 안은 **대사**. 따옴표가 닫힐 때까지 이어지므로
  **줄바꿈을 넘어가는 대사도 하나로 인식**합니다.
- `*…*` / `**…**` 안은 **강조 서술**.
- 줄 머리의 `이름:` 은 **캐릭터 이름**으로 보고, 이후 대사에 화자를 표시합니다.
- 나머지는 **서술**.

색상은 span 에 직접 칠하지 않고 CSS 변수(`--te-dialogue` 등)로 넘깁니다.
그래서 색을 바꿔도 본문을 다시 파싱하지 않고 즉시 반영되며,
사용자가 드래그해서 직접 준 색이 지워지지 않습니다.

한글 입력(IME) 조합 중에는 다시 그리지 않고, 결과가 달라질 때만 DOM 을 교체한 뒤
`lib/caret.ts` 로 커서를 원래 자리에 돌려놓습니다.

### 장평

CSS `font-stretch` 는 가변 글꼴이 아니면 동작하지 않아, 본문 영역을 `1/배율` 폭으로
넓혀 줄바꿈을 계산한 뒤 `scaleX()` 로 눌러 실제 폭에 맞춥니다.
그래서 장평을 바꾸면 줄바꿈 위치도 함께 바뀝니다.

### 이미지 영역 비율

지정한 비율은 **최소 크기**로 적용됩니다(`min-height`).
본문이 비율보다 길면 세로로 늘어나므로 글이 잘리지 않습니다.
(README 의 "너비를 지정하지 않아도 본문에 따라 유동적으로 늘어난다" 요구사항)

### 강조 기호 숨기기

`*…*` 의 별표는 따로 떼어 `data-te-role="mark"` span 으로 감싸고, 결과물에서는
`display: none` 으로 자리까지 없앱니다 (사이드바에서 끌 수 있음).

여는 기호를 막 입력했을 때 글자가 사라지면 곤란하므로, **닫는 짝이 같은 줄에
있을 때만** 강조로 인식합니다. `3 * 4` 처럼 홀로 쓴 별표는 그냥 글자로 남습니다.

### 테마를 바꿔도 본문이 남는 이유

메신저 테마로 바꾸면 에디터가 언마운트됩니다. 앱이 최신 HTML 을 ref 에 들고 있다가
다시 마운트될 때 그대로 돌려주므로, 편집한 본문과 서식이 살아남습니다.
(에디터는 타이핑 즉시 HTML 을 알리고, localStorage 저장만 지연시킵니다.)

### 화면 구조

포토샵처럼 **왼쪽 아이콘 레일 + 꺼내 쓰는 패널 하나** 구조입니다.

- 레일 아이콘을 누르면 그 패널만 열리고, 같은 아이콘을 다시 누르면 닫힙니다.
  아이콘만으로는 뭔지 모르므로 hover 하면 이름표가 뜹니다.
- 열려 있는 패널은 `settings.activePanel` 에 남아 다음에 열 때도 그대로입니다.
- 패널 오른쪽 가장자리를 드래그해 폭을 조절합니다(280–640px, 더블클릭하면 기본값).
  폭이 470px 을 넘으면 값 조절·색상 항목이 2열로 늘어섭니다(컨테이너 쿼리).
- 좁은 화면(900px 이하)에서는 패널이 작업 영역을 덮도록 띄웁니다.

상단 막대에는 **테마(기본/메신저)** 를 가운데,
**되돌리기·다시 실행·다크/라이트** 를 오른쪽에 둡니다. 셋 다 스크롤과 무관하게
늘 같은 자리에 있습니다. 되돌리기는 Ctrl+Z / Ctrl+Shift+Z 로도 됩니다.

앱 화면 색은 `:root[data-app-theme='light']` 에서 토큰만 다시 정의하며,
결과 이미지 색과는 무관합니다.

### 확대 / 축소

`transform: scale` 은 레이아웃 크기를 바꾸지 않습니다. 확대한 만큼 바깥 틀의 높이를
따로 잡아 주지 않으면 미리보기가 아래 도구 막대를 덮어버립니다.
`ResizeObserver` 로 실제 높이를 재서 `높이 × 배율` 로 자리를 만듭니다.
결과물 크기와는 무관하며, 저장에는 영향을 주지 않습니다.

### 스티커

본문 위에 얹는 이미지입니다. 위치·크기를 결과물 너비 대비 % 로 들고 있어 확대·축소를
해도 흔들리지 않습니다. 드래그 이동량은 화면 기준이라 배율로 나눠 실제 값으로 되돌립니다.
스티커 층은 `pointer-events: none` 이고 스티커 자신만 받으므로, 겹치지 않는 곳의
본문 편집은 그대로 됩니다. 선택 테두리와 손잡이는 `data-export-ignore` 라 결과물에 안 나옵니다.

### 팝업 안의 입력칸

팝업의 숫자 입력이나 드롭다운에 포커스가 가면 본문 선택이 풀려 팝업이 닫힙니다.
마지막 선택 Range 를 들고 있다가 명령 직전에 되돌려 놓고, 포커스가 팝업 안에 있는 동안은
닫지 않습니다.

### 본문을 평문으로 읽기

`innerText` 를 쓰면 화면에서 감춘 강조 기호(*)까지 빠져, 메신저 테마가 어디가 강조인지
알 수 없게 됩니다. 그래서 블록을 직접 훑어 줄 단위로 읽습니다(`readPlainText`).

### 되돌리기

브라우저 기본 실행취소는 본문 글자만 되돌리고 사이드바 서식은 그대로 둡니다.
본문 HTML 과 설정을 한 덩어리(Snapshot)로 묶어 함께 되돌립니다. 복원하는 동안에는
`restoring` 을 세워 기록이 다시 쌓이지 않게 합니다.

### 페이지 나눔

본문에 `data-te-page-break` 블록을 넣으면, 저장할 때 그 선을 기준으로 여러 장으로 쪼갭니다.
줄마다 `display` 를 껐다 켜며 한 장씩 캡처하고, 끝나면 반드시 되돌립니다.
contenteditable 이 첫 줄을 `<div>` 로 감싸지 않는 경우가 있어, 맨 위 텍스트 노드는
임시 span 으로 싸 두었다가 캡처 후 풀어 줍니다. PDF 는 한 파일에 여러 쪽으로 들어갑니다.

### 작업 내용 보관

localStorage 는 한 사이트당 5MB 안팎입니다. 배경 사진 한 장이면 한도를 넘고, 넘는
순간 **아무 소리 없이** 저장이 멈춰 새로고침할 때 작업이 통째로 사라집니다.
그래서 설정과 본문을 IndexedDB 에 담습니다. 예전 localStorage 기록은 첫 실행 때
한 번만 옮겨 옵니다. 그래도 저장이 막히면 화면 위에 경고를 띄웁니다.

읽기가 비동기라, **다 읽기 전에는 화면을 만들지 않습니다**(`StoreProvider` 가 막습니다).
에디터는 마운트할 때 내용을 한 번만 받으므로, 먼저 그려 버리면 빈 본문을 붙잡은 채
시작해 저장된 글을 덮어씁니다.

### 사용자 글꼴

한글 글꼴 파일은 보통 수 MB 라 localStorage 한도에 바로 걸립니다. 파일은 IndexedDB 에 두고
설정에는 목록만 남긴 뒤, 불러올 때 data: URL 로 `@font-face` 를 심습니다.
data: URL 이어야 내보내기(html-to-image)에서도 글꼴이 그대로 살아납니다.

### 사이드바 2열 배치

`.sidebar-scroll` 에 `container-type: inline-size` 를 걸고 컨테이너 쿼리로 전환합니다.
폭이 470px 을 넘으면 값 조절·색상 항목만 2열로 늘어서고, 나머지는 한 줄을 다 씁니다.

### 앱 색상 토큰

앱 화면 색은 전부 `--ui-*` 토큰을 거칩니다. 라이트 모드는 `:root[data-app-theme='light']`
에서 토큰만 다시 정의합니다. **최상위 요소**에 걸어야 `body` 가 물려주는 색까지 함께
바뀝니다 — `.app` 에만 걸면 body 를 그냥 상속받는 글자가 다크 모드 색으로 남습니다. 팝업·hover·토글처럼 눈에 잘 안 띄는 곳까지 토큰을 쓰지 않으면
한쪽 모드에서만 색이 어긋나므로, 새 UI 를 만들 때 색을 직접 적지 않습니다.

### 빈 줄 처리

문단 사이 간격은 `문단 간격` 옵션이 담당합니다. 붙여넣은 글의 빈 줄까지 그대로 두면
간격이 두 배로 벌어지므로, 기본적으로 붙여넣을 때 빈 줄을 정리합니다.
사이드바 위쪽의 `붙여넣을 때 빈 줄 정리` 로 끌 수 있습니다.

### 배경 영상 / GIF

- **위치**: `드래그로 위치 조절` 을 켜면 미리보기 위에 덮개가 생기고, 끌어서 이미지 위치를
  옮깁니다. 켜져 있는 동안에는 본문 편집이 막힙니다. `꽉 채우기` 일 때 가장 잘 보입니다.
- **영상**: 저장 시점의 재생 프레임을 캔버스로 떠서 캡처합니다. 원하는 장면에서 저장하세요.
- **GIF**: 미리보기에서는 움직이지만 이미지·PDF 로 저장하면 한 장면으로 고정됩니다.
- 외부 주소의 영상·이미지는 CORS 설정에 따라 캡처가 막힐 수 있습니다. 업로드 방식이 안전합니다.

### 메신저 테마

메신저는 **별도 화면이 아니라 같은 본문의 표시 방식**입니다.
에디터는 언제나 하나뿐이고, `.messenger-mode` 클래스가 붙으면 대사 span 만
말풍선 모양으로 보이게 CSS 가 바꿉니다. 그래서

- 메신저 상태에서도 편집·페이지 나눔·드래그 팝업이 그대로 동작하고,
- 볼드·색·글꼴 등 사용자가 준 서식이 전부 유지됩니다.

이름의 콜론(`이름:`)과 대사의 따옴표는 따로 감싸 두었다가 메신저에서만 감춥니다.
말풍선 간격은 `:has([data-te-role="dialogue"])` 로 **말풍선이 든 줄에만** 겁니다 —
모든 줄에 걸면 일반 문단 간격까지 함께 움직입니다.
'내 쪽' 정렬도 줄 전체에 `text-align` 을 주면 말풍선 아래 딸린 글까지 따라가므로,
말풍선과 이름에만 겁니다.

여러 줄에 걸친 대사는 줄마다 조각이 생깁니다. 이어지는 조각(`data-te-cont`)은 위쪽
여백·모서리를 없애고 앞 조각의 아래쪽도 깎아 한 덩어리로 보이게 하며, 조각마다
폭이 달라지지 않도록 묶음 전체의 너비를 맞춥니다.
'내 쪽' 캐릭터의 오른쪽 정렬은 `:has()` 로 줄 전체를 잡아 겁니다 —
그래서 모든 줄이 요소여야 하고, contenteditable 이 감싸 주지 않는 첫 줄은
`wrapLooseFirstLine` 이 `<div>` 로 감쌉니다.

역할 span 안에 끼워 넣는 래퍼(`.te-namesep`, `.te-quote`)는 재파싱 때 반드시
함께 벗겨야 합니다. 남겨 두면 `이름:` 패턴이 끊겨 이름이 다시 인식되지 않습니다.

## 배포

`main` 브랜치에 푸시하면 `.github/workflows/deploy.yml` 이 GitHub Pages 로 배포합니다.
저장소 **Settings → Pages → Source** 를 **GitHub Actions** 로 한 번 설정해야 동작합니다.

프로젝트 페이지(`/text-extract/`) 기준으로 경로가 잡혀 있습니다.
다른 경로에 올리려면 `VITE_BASE` 환경변수로 덮어쓰세요.

## 아직 안 된 것

- 이미지 영역(결과물) 크기를 **드래그 핸들**로 조절하는 기능 (지금은 숫자·슬라이더만)
- 스티커 회전·투명도는 값만 있고 조절 UI 가 없음
- 메신저 외 추가 테마
- 본문 편집 내역 되돌리기는 브라우저 기본 실행취소(Ctrl+Z)에 의존
